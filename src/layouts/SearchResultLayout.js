import { SearchResultPage }  from "../pages/SearchResultPage";

export const SearchResultLayout = ({searchKey}) => {
    return (
        <div>
            <SearchResultPage />
        </div>
    );
};

